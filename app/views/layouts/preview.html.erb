<!DOCTYPE html>
<html data-theme="<%= lookbook_display(:theme) %>">
<head>
  <title>Component Preview</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  
  <%= render "layouts/head" %>
</head>
<body>
  <div style="padding: <%= lookbook_display(:padding, "20px") %>">
    <div class="mx-auto" style="max-width: <%= lookbook_display(:max_width, "1400px") %>">
      <%= yield %>
    </div>
  </div>
</body>
</html>